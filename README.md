# 2026-1-SDM-Segunda-Noite-ZS-03-RN

## API

Copie `.env.example` para `.env` e ajuste a URL da API Django:

```text
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000
```

Ao testar em celular ou emulador, troque `127.0.0.1` pelo IP da maquina que esta rodando o backend.
